--- TEST: FIB-001
--- Runner: expect_compile_error = true
--- Area: Fibers
--- Priority: critical
--- Name: Typed fiber — missing return at the end → compile error S210
--- Expect:
---   This file SHOULD NOT compile.
---   Typed fiber without return at the end → S210.
--- .
--- Runner: expected non-zero compilation exit code, stderr contains "S210".

--- CRITICAL LINE — typed fiber without return:
fiber:i gen(-> i) {
    yield 1;
    yield 2;
    --- missing return — must be S210
};

>! "FAIL — S210 was not detected";
