--- TEST: SEC-001
--- Area: Security
--- Priority: critical
--- Name: store — path traversal (..) → halt.fatal
--- Expect:
---   store.read("../../etc/passwd") → halt.fatal, VM terminates.
---   Line AFTER invocation will NOT execute.
--- Runner: expect_fatal_exit = true

>! "[SEC-001] Attempting store.read with path traversal...";
s: content = store.read("../../etc/passwd");

>! "[SEC-001] FAIL — path traversal did not trigger halt.fatal";
