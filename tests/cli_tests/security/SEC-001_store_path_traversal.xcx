--- TEST: SEC-001
--- Area: Security
--- Priority: critical
--- Name: store — path traversal (..) → halt.fatal
--- Expect:
---   store.read("../../etc/passwd") → halt.fatal, VM terminuje.
---   Linia PO wywołaniu się NIE wykona.
--- Runner: expect_fatal_exit = true

>! "[SEC-001] Próba store.read z path traversal...";
s: content = store.read("../../etc/passwd");

>! "[SEC-001] FAIL — path traversal nie wywołał halt.fatal";
