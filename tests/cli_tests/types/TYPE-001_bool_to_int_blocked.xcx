--- TEST: TYPE-001
--- Area: Types
--- Priority: high
--- Name: Rzutowanie b→i zablokowane — type error
--- Expect:
---   i(bool) → type error (compile lub runtime).
---   bool + int → type error.
--- Runner: expect_compile_error = true

b: flag = true;

--- LINIA KRYTYCZNA — zablokowane rzutowanie:
i: n = i(flag);

>! "FAIL — b→i rzutowanie nie wywołało błędu";
