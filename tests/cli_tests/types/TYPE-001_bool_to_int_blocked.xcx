--- TEST: TYPE-001
--- Area: Types
--- Priority: high
--- Name: Casting b→i blocked — type error
--- Expect:
---   i(bool) → type error (compile or runtime).
---   bool + int → type error.
--- Runner: expect_compile_error = true

b: flag = true;

--- CRITICAL LINE — blocked cast:
i: n = i(flag);

>! "FAIL — b→i casting did not trigger error";
