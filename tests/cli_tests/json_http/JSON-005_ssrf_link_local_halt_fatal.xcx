--- TEST: JSON-005
--- Area: JSON/HTTP
--- Priority: high
--- Name: SSRF — blocking link-local (169.254.x.x) → halt.fatal
--- Expect:
---   Request to 169.254.x.x → halt.fatal, VM terminates.
---   Line after invocation will NOT execute.
--- Runner: expect_fatal_exit = true

>! "[JSON-005] Attempting connection to link-local SSRF address...";
json: r = net.get("http://169.254.169.254/latest/meta-data/");

>! "[JSON-005] FAIL — net.get() to link-local did not trigger halt.fatal";
