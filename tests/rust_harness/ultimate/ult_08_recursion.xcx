--- Simulation: manually create request and check handler logic
fiber handle_ping(json: req -> json) {
    s: method;
    req.bind("method", method);
    json: resp <<< {"pong": true, "method": ""} >>>;
    resp.set("method", method);
    yield net.respond(200, resp);
    return <<<{}>>>;
};