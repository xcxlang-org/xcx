fiber inner(i: x -> i) {
    yield x * 2;
    yield x * 3;
    return 0;
};

fiber outer(i: val -> i) {
    fiber:i: sub = inner(val);
    i: a = sub.next();
    yield a;
    i: b = sub.next();
    yield b;
    return 0;
};

fiber:i: f = outer(5);
>! f.next();
>! f.next();
