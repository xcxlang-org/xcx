array:s: buf {"", "", "", "", ""};
i: t0 = perf.us();

for i in 1 to 100000 do;
    buf.update(0, buf.get(0) + "a");
end;

i: t1 = perf.us();
f: ms = f(t1 - t0) / 1000.0;
i: len = buf.get(0).length;
>! "Array elem str append 100k: " + s(ms) + " ms  |  len=" + s(len);